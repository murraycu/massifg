
To compile and run the program, do:
./autogen.sh
make
./bin/massifg FILENAME

To compile and run the unit tests, do:
gcc tests/parser.c -o test_parser `pkg-config --cflags --libs glib-2.0`
gtester test_parser --verbose


